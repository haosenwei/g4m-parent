server:
  port: 8081

spring:
  application:
    name: configtest
  profiles:
    active: dev
  cloud:
    config:
      profile: dev
  rabbitmq: #RabbitMQ相关的配置
    host: 127.0.0.1
    port: 5672
    username: guest
    password: guest
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
management:
  endpoints:
    web:
      exposure:
        include: bus-refresh